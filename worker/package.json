{
  "name": "ipixel-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Public Cloudflare Worker for ipixel-mcp: OAuth 2.1 (DCR) AS/RS + authenticating proxy to the tailnet origin (Phase 4).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.3",
    "vitest": "~3.0.0",
    "wrangler": "^4.20.0"
  }
}
